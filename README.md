# Finding Lunch (午餐吃什麼？)

這是一個幫助你解決「午餐吃什麼」難題的 React 應用程式。它透過 Google Sheets 作為後端資料庫，讓你輕鬆管理餐廳名單，並透過轉盤隨機抽出今天的午餐選擇。

## 功能特色
- 🎲 隨機轉盤抽出午餐
- 📝 使用 Google Sheets 輕鬆管理餐廳資料
- 🏷️ 支援餐廳圖片、價格、地圖連結與標籤
- 📱 響應式設計 (Mobile First)

## 🚀 快速開始 (Quick Start)

### 1. 準備 Google Sheets (後端資料庫)

1. 建立一個新的 [Google Sheet](https://sheets.new)。
2. 將下方的工作表標籤 (Tab Name) 重新命名為 **`商家`** (請務必完全一致，否則程式會讀不到)。
3. 在第一列 (Row 1) 建立標題，依序為：
    - **A欄**: 餐廳名稱 (Name) - *必填*
    - **B欄**: 價格區間 (Price)
    - **C欄**: 營業時間/狀態 (Status)
    - **D欄**: Google Maps 連結 (Map Url)
    - **E欄**: 圖片連結 (Image Url)
    - **F欄**: 標籤 (Tags)

    *(建議先輸入幾筆測試資料)*

### 2. 設定 Google Apps Script (API 服務)

1. 在 Google Sheet 中，點選選單列的 **擴充功能 (Extensions)** > **Apps Script**。
2. 刪除編輯器中預設的程式碼。
3. 將專案中的 `backend_script.gs` 內容完整複製並貼上到編輯器中。
4. 點擊磁片圖示 💾 儲存專案 (可隨意命名，如 `Lunch API`)。
5. 點擊右上角的 **部署 (Deploy)** > **新增部署 (New deployment)**。
6. 在「選取類型 (Select type)」旁的齒輪圖示 ⚙️ 選擇 **網頁應用程式 (Web app)**。
7. 設定如下：
    - **說明 (Description)**: (選填，例如 v1)
    - **執行身份 (Execute as)**: **我 (Me)**
    - **誰可以存取 (Who has access)**: **任何人 (Anyone)** (⚠️ 這點很重要，讓前端可以讀取資料)
8. 點擊 **部署 (Deploy)**。
9. 授權存取：
    - 點擊「核對權限 (Review permissions)」。
    - 選擇你的 Google 帳號。
    - 如果出現「Google 尚未驗證這個應用程式」，點擊 **進階 (Advanced)** > **前往 (Go to ...)** (不安全)。
    - 點擊 **允許 (Allow)**。
10. 複製 **網頁應用程式網址 (Web app URL)** (通常以 `https://script.google.com/macros/s/.../exec` 結尾)。

### 3. 設定前端專案

1. 確保電腦已安裝 [Node.js](https://nodejs.org/)。
2. 複製專案到本地：
    ```bash
    git clone <repository-url>
    cd findingLunch
    ```
3. 安裝依賴套件：
    ```bash
    npm install
    ```
4. 設定環境變數：
    - 在專案根目錄建立 `.env.local` 檔案 (或直接修改 `.env`)。
    - 加入以下內容，並填入步驟 2 取得的 URL：
      ```env
      VITE_GOOGLE_APP_SCRIPT_URL=你的_Google_Apps_Script_URL
      ```
5. 啟動開發伺服器：
    ```bash
    npm run dev
    ```
6. 打開瀏覽器訪問顯示的 Local URL (例如 `http://localhost:5173`)。

---

## 開發指令

- `npm run dev`: 啟動開發伺服器
- `npm run build`: 建置生產版本
- `npm run preview`: 預覽生產版本

## 專案結構

- `/src`: 前端 React 程式碼
- `backend_script.gs`: Google Apps Script 後端程式碼備份

## 📦 部署 (Deployment)

本專案已設定 GitHub Actions，可自動部署至 GitHub Pages。

### 設定步驟

1. **設定 Github Secrets**:
   - 進入 GitHub 儲存庫頁面，點選 **Settings** > **Secrets and variables** > **Actions**。
   - 點擊 **New repository secret**。
   - **Name**: `VITE_GOOGLE_APP_SCRIPT_URL`
   - **Value**: 填入你的 Google Apps Script 網頁應用程式網址。

2. **設定 GitHub Pages**:
   - 點選 **Settings** > **Pages**。
   - 在 **Build and deployment** > **Source** 選擇 **GitHub Actions**。

3. **設定 `vite.config.js` (若非根網域部署)**:
   - 如果你的 GitHub Pages 網址是 `https://<username>.github.io/<repo-name>/`，請修改 `vite.config.js`：
     ```javascript
     export default defineConfig({
       plugins: [react()],
       base: '/<repo-name>/', // 例如: '/findingLunch/'
     })
     ```
   - 若是部署在 `https://<username>.github.io/`，則無需設定 `base`。

設定完成後，每次推送到 `main` 分支時，GitHub Actions 就會自動打包並部署更新。
